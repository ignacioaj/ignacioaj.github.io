---
// Divider.astro
// Componente divisor de secciones con 0 y 1 sin fondo y números más finos.
---

<div class="divider">
  <span>0</span><span>1</span><span>0</span><span>1</span><span>0</span><span>1</span><span>0</span><span>1</span>
</div>

<style>
.divider {
  display: flex;
  justify-content: center;
  gap: 0.5rem;
  padding: 2rem 0;
  /* Eliminamos el fondo */
  background: transparent;
  font-weight: normal; /* números más finos */
  font-family: monospace;
  color: #000000; /* opcional: puedes cambiar el color si quieres que contraste más */
  font-size: 1.5rem;
}

.divider span {
  animation: float 2s infinite alternate;
}

@keyframes float {
  0% { transform: translateY(0); }
  100% { transform: translateY(-10px); }
}
</style>
